# CookieCloud (recommended)
COOKIECLOUD_UUID=YOUR_UUID
COOKIECLOUD_PASSWORD=YOUR_PASSWORD

# If you run CookieCloud via this repo's docker-compose, you can keep the default.
# If CookieCloud runs elsewhere, set the full base URL here.
COOKIECLOUD_SERVER_URL=http://127.0.0.1:8088

# Optional: for faster-whisper model downloads in regions where Hugging Face is slow/unreachable.
# Example: https://hf-mirror.com
HF_ENDPOINT=https://huggingface.co

